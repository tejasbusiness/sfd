{# Homepage pricing plans. Plan cards reuse the Pricing page's .pricing-summary__* classes (assets/css/components.css) so both pages stay identical. #}
<section class="section pricing-highlight">
  <div class="container">
    <h2>{{ section.heading }}</h2>
    {% if section.intro %}<p>{{ section.intro }}</p>{% endif %}
    <div class="pricing-summary__plans">
      {% for plan in plans.plans %}
      <div class="card pricing-summary__plan {{ 'pricing-summary__plan--recommended' if plan.recommended else '' }}" data-reveal>
        {% if plan.recommended %}<p class="pricing-summary__tag">★ Best Value</p>{% endif %}
        <h3>{{ plan.name }}</h3>
        {% if plan.idealFor %}<p class="pricing-summary__ideal">{{ plan.idealFor }}</p>{% endif %}
        {% if plan.priceMonthly %}
        <p class="pricing-summary__price">{{ plan.priceMonthly }}/mo</p>
        {% endif %}
        <p>{{ plan.summary }}</p>
        <ul class="pricing-summary__includes">
          {% for item in (plan.homeHighlights or plan.includes) %}
          <li>
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true">
              <path d="M20 6L9 17l-5-5" stroke-linecap="round" stroke-linejoin="round" />
            </svg>
            {{ item }}
          </li>
          {% endfor %}
        </ul>
        <a
          class="button {{ 'button--accent' if plan.recommended else 'button--secondary' }} pricing-summary__plan-cta"
          href="/free-preview/"
          aria-label="Start with {{ plan.name }}: request your free website preview"
        >Start with {{ plan.name }}</a>
      </div>
      {% endfor %}
    </div>
    {% if section.cta %}
    <p class="pricing-highlight__more"><a href="{{ section.cta.href }}">{{ section.cta.label }} <span class="pricing-highlight__more-arrow" aria-hidden="true">&rarr;</span></a></p>
    {% endif %}
  </div>
</section>
