# Claude project instructions

Read `AGENTS.md` and `docs/AI-HANDOFF.md` completely before doing any work. Read `docs/ROADMAP.md` when the task affects priorities or milestone status, and `docs/DECISIONS.md` before making a durable project decision. Follow these files as repository-level instructions.

Use `docs/AI-HANDOFF.md` to continue the current task rather than reconstructing the project from chat history. Update that file before returning control to Codex or the user.

Treat a fresh chat as one bounded task. Inspect only the files needed for that task, preserve uncommitted work, verify claims against the repository, and do not infer missing business facts.
